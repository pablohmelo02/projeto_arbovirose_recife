requests>=2.31,<3
boto3>=1.34,<2
python-dotenv>=1.0,<2
pytest>=8.0,<9

# Profiling da Bronze e transformação Silver: leitura/escrita tabular e
# Parquet (Silver = Parquet, Bronze continua CSV).
pandas>=2.2,<4
pyarrow>=16.0,<26

# Domínio Território: leitura de GeoJSON, geometria (validade, área,
# centroide) e reprojeção de CRS. geopandas depende de shapely e pyproj, mas
# ambos são fixados explicitamente porque o código os importa diretamente
# (shapely para checagem de validade de geometria, pyproj indiretamente via
# reprojeção de CRS no geopandas).
geopandas>=1.0,<2
shapely>=2.0,<3
pyproj>=3.6,<4

# Usado apenas em testes: sobe um servidor HTTP que fala o protocolo S3 de
# verdade, permitindo validar o MinioClient sem precisar do MinIO/Docker.
moto[server]>=5.0,<6

# Usado apenas em testes: mocka respostas HTTP (InmetClient/ApacClient) para
# testar tratamento de erro sem depender da APAC/INMET estarem no ar. Já
# vinha como dependência transitiva do moto; fixado aqui porque os testes o
# importam diretamente.
responses>=0.25,<1

# Visualizações de validação da Gold (`python -m src.analyze_gold`): séries
# temporais, cobertura, completude e mapa coroplético por bairro. Backend
# não-interativo (Agg) — gera PNG em reports/gold_analysis/, não abre janela
# nem serve dashboard.
matplotlib>=3.8,<4

# Dashboard web (EDA interativa), publicado a partir da Gold já pronta —
# ver dashboard/ e src/eda/. Streamlit é o framework da aplicação (roda
# local via `streamlit run dashboard/app.py` e é publicável no Streamlit
# Community Cloud); Plotly é a única biblioteca de gráficos/mapa da
# aplicação (choropleth via GeoJSON, sem Folium/PyDeck em paralelo — ver
# decisão registrada em CLAUDE.md).
streamlit>=1.38,<2
plotly>=5.20,<6

# Usado só pela linha de tendência (OLS) dos scatter plots de lag
# clima x casos na página "Clima x Arboviroses" (`px.scatter(...,
# trendline="ols")` depende disso internamente).
statsmodels>=0.14,<1

# Etapa de Machine Learning (`src/ml/`): baseline de alerta antecipado de
# dengue. LogisticRegression (modelo interpretável) e
# HistGradientBoostingClassifier (modelo de árvores nativo do sklearn, sem
# exigir XGBoost/LightGBM como dependência extra) cobrem as seções 23-24 do
# pedido com a menor superfície de dependência nova possível.
scikit-learn>=1.4,<2

